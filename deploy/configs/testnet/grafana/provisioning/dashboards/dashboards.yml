# Grafana Dashboard Provisioning

apiVersion: 1

providers:
  - name: 'BlueBlocks Dashboards'
    orgId: 1
    folder: 'BlueBlocks'
    folderUid: 'blueblocks'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
